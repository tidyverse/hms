# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: false
cache: packages

after_success:
  - Rscript -e 'devtools::install_github("jimhester/covr@v2"); covr::codecov()'
